import numpy as np


class RenderingParametersForwardMathematicalModel:
    def __init__(self):

    def MathematicalCore(self):
        i = 0
        markIdx = 0
        ret = 0
        idx = 0
        start_num = 0 # マーク情報領域
        mNum = 0 # マーク数
        mark_id = 0 # マークID  格納
        # 　パラメータチェック(分割エリア数)
        divCnt = 0
        divNum = 0

        divNum = divInfo->divnum_x * divInfo->divnum_y;


if __name__ == "__main__":
    pr("Start calculating the alignment paramters")
